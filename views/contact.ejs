<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Contact Dorothy Care</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <header>
    <div class="navbar">
      <div class="logo">
        <span>Dorothy Care</span>
      </div>
      <nav class="nav-links">
        <a href="/">Home</a>
        <a href="/contact">Contact</a>
      </nav>
    </div>
  </header>

  <div class="contact-container">
    <h1>Contact Dorothy Care</h1>

    <% if (submitted) { %>
      <p class="success-message">Your message has been sent successfully!</p>
    <% } %>

    <form action="/contact" method="POST" class="contact-form">
      <div class="form-group">
        <label>Name:</label>
        <input type="text" name="name" required />
      </div>

      <div class="form-group">
        <label>Email:</label>
        <input type="email" name="email" required />
      </div>

      <div class="form-group">
        <label>Message:</label>
        <textarea name="message" required></textarea>
      </div>

      <button type="submit" class="submit-btn">Send Message</button>
    </form>
  </div>

  <!-- Footer Section -->
  <footer>
    <p>&copy; 2025 Dorothy Care. All Rights Reserved.</p>
  </footer>

  <!-- Link to the JS file -->
  <script src="/js/main.js"></script>

  <script>
    function toggleReadMore() {
      var fullText = document.getElementById('full-description');
      var readMoreLink = document.getElementById('read-more');
      
      // Toggle the display of the text and change the link text
      if (fullText.style.display === 'none') {
        fullText.style.display = 'block';
        readMoreLink.innerText = 'Read Less';
      } else {
        fullText.style.display = 'none';
        readMoreLink.innerText = 'Read More';
      }
    }
  </script>

  <style>
    /* General Styling */
    .contact-container {
      max-width: 600px;
      margin: 50px auto;
      padding: 30px;
      background: #fff;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
      text-align: center;
    }

    h1 {
      font-size: 28px;
      color: #333;
      margin-bottom: 20px;
      font-weight: 600;
    }

    .success-message {
      color: #2d9c2d;
      background: #eaf7ea;
      padding: 10px;
      border-radius: 5px;
      margin-bottom: 20px;
    }

    /* Form Styling */
    .contact-form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      text-align: left;
    }

    .form-group label {
      font-size: 16px;
      color: #555;
      margin-bottom: 5px;
      font-weight: 500;
    }

    .form-group input,
    .form-group textarea {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 16px;
      transition: 0.3s ease-in-out;
    }

    .form-group input:focus,
    .form-group textarea:focus {
      border-color: #007bff;
      outline: none;
      box-shadow: 0 0 5px rgba(0, 123, 255, 0.2);
    }

    .form-group textarea {
      height: 120px;
      resize: none;
    }

    /* Button Styling */
    .submit-btn {
      background: #007bff;
      color: #fff;
      padding: 12px;
      border: none;
      border-radius: 5px;
      font-size: 18px;
      cursor: pointer;
      transition: 0.3s ease-in-out;
    }

    .submit-btn:hover {
      background: #0056b3;
    }
  </style>

</body>
</html>
